[] reduce the logo size used in display to 64x64 at most, we could host more logos. Given screen is 480x222, we could change the launcher layout as: top is status bar, showing current time, date, battery, wifi indicator, bluetooth indicator; middle is a a row of logos for available apps; bottom is the title of the selected apps.
[x] polish the display of weather app
[x] calendar app is missing from launcher;
[x] keyboard mapping (short cut). use space as modifier, space + w scroll up, space + s scroll down, space + p previous page, space + n next page.
[x] micro sd card
[] dictionary file
[] gps app layout: default page uses two column layout, one column for gps coordinate and others; the other column for gps status. next page shows gps tracking related functions, still two column layout, left column for track status (start/stop, save) and main column visualizes the track.
[] calculator app: use sym + x ($) and sym + v (?) as ( and ). improve the layout of calculator to use the whole area for showing the input and results.


